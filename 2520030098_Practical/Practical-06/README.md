A signal is a notification sent to a process.

A signal handler is a function that handles the received signal.

The program captures three signals:

SIGINT – Interrupt signal

SIGTERM – Termination signal

SIGUSR1 – User-defined signal

The handler function executes automatically when a signal is received
