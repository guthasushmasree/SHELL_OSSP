
Control transition between user space and kernel space: User Space -> System Call -> Kernel Space -> File Operation -> User Space

open(): Opens the file

read(): Reads the file contents

write(): write data into file

close(): closes the file
